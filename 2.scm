(define-library (lib gl 2.0)
(import
   (scheme core)
   (lib gl)
   (lib gl shading)
   (OpenGL 2.0))

(export
   (exports (lib gl))
   (exports (lib gl shading))
   (exports (OpenGL 2.0)))

(begin ))
